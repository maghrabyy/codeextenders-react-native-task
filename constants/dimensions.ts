import {
  responsiveHeight,
  responsiveWidth,
  responsiveFontSize,
} from 'react-native-responsive-dimensions';

const resW = responsiveWidth;
const resH = responsiveHeight;
const resFont = responsiveFontSize;

export { resW, resH, resFont };
